projection;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for /EACM/C_ZPRFISC alias /eacm/cZprfisc
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action CalculateFisc;
  use action PostFisc;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}